import { createContext } from "react"

export const CountContext = createContext(0);

// it is the tele-porter that will teleport variables from one component to another. 
// it will rectify the problem of prop drilling