// for input checks primarily zod library is used.
// schema is the inputs that we enter
// parsing is perfoming checks on the inputs provided.

const express = require("express");
const zod = require("zod")